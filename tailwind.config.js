/** @type {import('tailwindcss').Config} */
module.exports = {
 content: ['./*.{html,js}'], // Scan all HTML and JS files in 
  darkMode: 'class', // Utiliser la classe 'dark' pour basculer le mode sombre
  theme: {
    extend: {},
  },
  plugins: [],
};

